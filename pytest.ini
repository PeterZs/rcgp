[pytest]
testpaths = tests
addopts = -ra
markers =
    compile_fail: compile-only tests that are expected to fail
    compile_pass: compile-only tests that are expected to pass
    runtime: run-time test suite for the C++ test binary
